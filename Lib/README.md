# Utilities.DotNet

## About

The _Utilities.DotNet_ package provides useful generic utilities for .NET.

## Usage

TBD

## Generate NuGet Package

The NuGet package to be published supports multiple configurations (Release and Profile).

In order to generate the NuGet package properly, both configurations need to be built first, then once both configurations are built the NuGet package can be generated (from inside VS or using the `dotnet pack` command).
